<p style="color: green"><%= notice %></p>

<h1>Cities</h1>

<div id="cities">
  <% @cities.each do |city| %>
    <%= render city %>
  <% end %>
</div>

<div class="show-item">
  <%= link_to "New city", new_city_path %>
  <%= link_to "Back to items", items_path %>
</div>
